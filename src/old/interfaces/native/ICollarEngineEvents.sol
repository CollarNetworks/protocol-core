// SPDX-License-Identifier: MIT
pragma solidity ^0.8.18;

interface ICollarEngineEvents {
    event TestEvent(address admin);
    //event TradeAccepted(address vault, address newDexRouter);
    //event TradeRejected(address rejector, address client, string reason);
}
